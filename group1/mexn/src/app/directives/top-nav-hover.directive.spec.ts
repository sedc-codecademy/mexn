import { TopNavHoverDirective } from './top-nav-hover.directive';

describe('TopNavHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new TopNavHoverDirective();
    expect(directive).toBeTruthy();
  });
});
